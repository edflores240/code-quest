public class test_compile {
    private static class Player {
        public enum Direction {
            DOWN, LEFT, RIGHT, UP
        }
        
        public enum ActionState {
            IDLE, WALKING, ACTION
        }
    }
}
